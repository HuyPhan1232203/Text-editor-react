/**
 * @file /src/constants/pageSides.ts
 * @name PageSides
 * @description Constants for page sides in the editor.
 */

import type { PageSide } from '../types/page'

export const pageSides: PageSide[] = ['top', 'right', 'bottom', 'left']
